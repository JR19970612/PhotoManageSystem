<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ydb.dao.IAdminDao">
    <resultMap id="BaseResultMap" type="com.ydb.entity.Admin">
        <id column="admin_id" property="adminId" jdbcType="INTEGER"/>
        <result column="admin_name" property="adminName" jdbcType="CHAR"/>
        <result column="admin_password" property="adminPassword" jdbcType="CHAR"/>
    </resultMap>
    <sql id="Base_Column_List">
    admin_id, admin_name, admin_password
  </sql>
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select
        <include refid="Base_Column_List"/>
        from admin
        where admin_id = #{adminId,jdbcType=INTEGER}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from admin
    where admin_id = #{adminId,jdbcType=INTEGER}
  </delete>
    <insert id="insertAdmin" parameterType="com.ydb.entity.Admin">
        insert into admin (admin_id, admin_name, admin_password
        )
        values (#{adminId,jdbcType=INTEGER}, #{adminName,jdbcType=CHAR}, #{adminPassword,jdbcType=CHAR}
        )
        <selectKey keyProperty="adminId" order="AFTER" resultType="int">
            select @@identity
        </selectKey>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.ydb.entity.Admin">
        update admin
        <set>
            <if test="adminName != null">
                admin_name = #{adminName,jdbcType=CHAR},
            </if>
            <if test="adminPassword != null">
                admin_password = #{adminPassword,jdbcType=CHAR},
            </if>
        </set>
        where admin_id = #{adminId,jdbcType=INTEGER}
    </update>
    <update id="updateByPrimaryKey" parameterType="com.ydb.entity.Admin">
    update admin
    set admin_name = #{adminName,jdbcType=CHAR},
      admin_password = #{adminPassword,jdbcType=CHAR}
    where admin_id = #{adminId,jdbcType=INTEGER}
  </update>
</mapper>